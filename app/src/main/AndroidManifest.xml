<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CafeApp"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RouterActivity"
            android:exported="false" />
        <activity
            android:name=".SetupActivity"
            android:exported="false" />
        <activity android:name=".ui.staff.StaffActivity" />
        <activity android:name=".ui.staff.StaffDashboardActivity" />
        <activity android:name=".ui.staff.ActiveOrdersActivity" />
        <activity android:name=".ui.staff.CartDetailActivity" />
        <activity android:name=".ui.staff.StaffTableStatusActivity" />
        <activity android:name=".ui.admin.AdminActivity" />
        <activity android:name=".ui.admin.ManageStockActivity" />
        <activity android:name=".ui.admin.ManageTablesActivity" />
    </application>

</manifest>